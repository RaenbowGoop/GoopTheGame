using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ContentScaler : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {
        //40 px from top
        //int numOfRows = inventory.container.Items.Count / NUMBER_OF_COLUMN;
        //int newYTransform = 40 * 2 + 210 * numOfRows;
        //contentPanel.sizeDelta = new Vector2(0, newYTransform);
    }
}
